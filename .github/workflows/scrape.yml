name: Scrape Evertale Units

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 0" # Sunday 06:00 UTC

# IMPORTANT: allow the workflow token to write (helps even if you use PAT)
permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          # IMPORTANT: don't keep the default bot credentials
          persist-credentials: false
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Clean & install dependencies
        working-directory: scraper
        run: |
          rm -rf node_modules package-lock.json
          npm install

      - name: Run unit list scraper
        working-directory: scraper
        run: node scrape_unit_list.js

      - name: Run unit page scraper
        working-directory: scraper
        run: node scrape_unit_pages.js

      - name: Move generated units.json
        run: |
          mkdir -p data
          mv scraper/units.json data/units.json

      - name: Commit updated units.json
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add data/units.json
          git diff --cached --quiet || git commit -m "Update units.json"

      - name: Push changes using PAT
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          if [ -z "$GH_PAT" ]; then
            echo "GH_PAT secret is missing or empty"
            exit 1
          fi

          # Force origin to use PAT (this is the key)
          git remote set-url origin "https://x-access-token:${GH_PAT}@github.com/TheRealNodder/Evertale-Optimizer.git"
          git push origin HEAD:main
